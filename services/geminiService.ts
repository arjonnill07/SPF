// Service disabled as AI features are currently inactive.
// import { GoogleGenAI } from "@google/genai";
// import { NewsArticle } from "../types";

export const generateNewsReport = async (articles: any[]): Promise<string> => {
  return "AI Reporting is currently disabled.";
};
